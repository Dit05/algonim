# Algonim

*My Thesis Project*

## How to build

Go into the `algorithm` directory and run the `build` script with `npm`: `npm run build`

## How to use

After building, you can simply open `test.html` in a web browser.
